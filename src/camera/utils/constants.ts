/**
 * @module camera/utils/constants
 * @internal
 */

/** 2π — full circle in radians. */
export const PI_2 = Math.PI * 2;

/** π/2 — right angle in radians. */
export const PI_HALF = Math.PI / 2;

/** Epsilon for rest-detection comparisons. */
export const EPS = 1e-7;
